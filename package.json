{
  "name": "@evankirsch/github-langs-widget",
  "version": "0.0.0-snapshot.1",
  "description": "Widget that provides language percents as percent of all languages in a given users public github projects",
  "repository": "github:evankirsch/github-langs-widget",
  "scripts": {
    "build": "tsc --build",
    "init": "tsc --init",
    "test": ":",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "octokit": "^5.0.5",
    "typescript": "^5.9.3"
  }
}

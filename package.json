{
  "name": "@evan-kirsch/github-langs-widget",
  "version": "1.0.0",
  "description": "Widget that provides language percents as percent of all lanugages in a given users public github projects",
  "scripts": {
    "build": "tsc --build",
    "init": "tsc --init",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "octokit": "^5.0.5",
    "tsc": "^2.0.4",
    "typescript": "^5.9.3"
  }
}